<?xml version="1.0" encoding="utf-8"?>
<!-- 4×3 小部件布局 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg_light"
    android:orientation="vertical"
    android:padding="14dp">

    <!-- 顶部：Tab 栏 (今日待办 / 时限任务) + 添加按钮 -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp">

        <!-- 药丸 Tab 分段控制器容器 -->
        <LinearLayout
            android:id="@+id/widget_tab_container"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:background="@drawable/widget_tab_container"
            android:orientation="horizontal"
            android:padding="2dp">

            <TextView
                android:id="@+id/widget_tab_today"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:gravity="center"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:text="今日待办"
                android:textSize="13sp"
                android:textStyle="bold"
                android:textColor="@color/widget_tab_selected_text"
                android:background="@drawable/widget_tab_selected" />

            <TextView
                android:id="@+id/widget_tab_timed"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:gravity="center"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:text="时限任务"
                android:textSize="13sp"
                android:textColor="@color/widget_tab_unselected_text"
                android:background="@android:color/transparent" />

        </LinearLayout>

        <ImageButton
            android:id="@+id/widget_btn_add"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="添加任务"
            android:src="@drawable/ic_widget_add" />
    </RelativeLayout>

    <!-- 任务列表容器（ListView 支持滚动浏览） -->
    <ListView
        android:id="@+id/widget_task_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:scrollbarStyle="outsideOverlay"
        android:clipToPadding="false"
        android:fadingEdge="vertical"
        android:fadingEdgeLength="8dp" />

    <!-- 空状态提示 -->
    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="16dp"
        android:text="今天没有待办任务"
        android:textColor="@color/widget_text_secondary"
        android:textSize="13sp" />

    <!-- 底部：剩余数量 -->
    <TextView
        android:id="@+id/widget_footer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:gravity="center"
        android:textColor="@color/widget_text_secondary"
        android:textSize="12sp" />

</LinearLayout>
